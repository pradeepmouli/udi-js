{
    "dependencies": {
        "@matter/main": "0.11.8"
    },
    "devDependencies": {
        "@types/node": "^22.8.7",
        "typescript": "~5.7.2"
    },
    "name": "matter-app",
    "version": "1.0.8-alpha.0",
    "type": "module",
    "scripts": {
        "prepare": "tsc",
        "build": "tsc",
        "app": "node --enable-source-maps dist/BridgedDevicesNode.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "node -e 'fs.rmSync(\"dist\", { recursive: true, force: true })'"
    },
    "private": true,
    "description": "Bridge for multiple OnOff light/sockets with a CLI command execution interface",
    "author": "user.name=Pradeep Mouli <user.email=pmouli@mac.com>"
}
